
// var express = require('express');
// var router = express.Router();
// var passport = require('../appDomain/authentication').passport;

// var domain = require('../appDomain');

// router.get('/', function(req, res) {
//   res.render('create', { title: 'Create profile' , message: req.flash('createMessage') } );
// });

// router.post('/', passport.authenticate('local-create', {
//   successRedirect: '/', // redirect to success page (will be profile page or event map page)
//   failureRedirect: '/create', // redirect back to the login page
//   failureFlash: true // allow flash messages
// }));


// module.exports = router;
